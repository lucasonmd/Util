<UserControl x:Class="HmiViewer.Controls.VideoSlotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <!--
        VideoSlotControl: 단일 영상 슬롯.
        IsSelected=True 이면 네온 청록 테두리 강조.
        실제 영상 삽입 시 ContentPresenter(VideoHost)에 HWND 등을 바인딩.
    -->
    <Border x:Name="OuterBorder"
            Background="Transparent"
            BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="{StaticResource BorderNormalBrush}"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                        <Setter Property="BorderBrush" Value="{StaticResource BorderSelectedBrush}"/>
                        <Setter Property="BorderThickness" Value="2"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <!-- 영상 삽입 영역: 투명, 향후 D3D/HWND 오버레이 연동 -->
            <Border x:Name="VideoHost"
                    Background="Transparent"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>

            <!-- 채널 레이블 -->
            <TextBlock Text="{Binding ChannelLabel,
                               RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Style="{StaticResource SectionLabelStyle}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Opacity="0.5"/>
        </Grid>
    </Border>
</UserControl>

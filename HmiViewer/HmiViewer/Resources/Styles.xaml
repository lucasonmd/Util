<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================== HMI Button ===================== -->
    <Style x:Key="HmiButtonStyle" TargetType="Button">
        <Setter Property="Background"   Value="{StaticResource BgPanelBrush}"/>
        <Setter Property="Foreground"   Value="{StaticResource NeonBlueBrush}"/>
        <Setter Property="BorderBrush"  Value="{StaticResource BorderNormalBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily"   Value="Consolas"/>
        <Setter Property="FontSize"     Value="11"/>
        <Setter Property="FontWeight"   Value="Bold"/>
        <Setter Property="Cursor"       Value="Hand"/>
        <Setter Property="Padding"      Value="4,2"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource NeonBlueBrush}"/>
                            <Setter TargetName="Root" Property="Background"  Value="{StaticResource BgSurfaceBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource NeonCyanBrush}"/>
                            <Setter TargetName="Root" Property="Background"  Value="#22004466"/>
                            <Setter Property="Foreground" Value="{StaticResource NeonCyanBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== Func Button (상단 FUNC1~8) ===================== -->
    <Style x:Key="FuncButtonStyle" TargetType="Button" BasedOn="{StaticResource HmiButtonStyle}">
        <Setter Property="Height"  Value="36"/>
        <Setter Property="FontSize" Value="10"/>
    </Style>

    <!-- ===================== Side Button (F1~F12) ===================== -->
    <Style x:Key="SideButtonStyle" TargetType="Button" BasedOn="{StaticResource HmiButtonStyle}">
        <Setter Property="Height" Value="44"/>
        <Setter Property="Width"  Value="64"/>
        <Setter Property="FontSize" Value="11"/>
    </Style>

    <!-- ===================== Bottom Button (F13~F20) ===================== -->
    <Style x:Key="BottomButtonStyle" TargetType="Button" BasedOn="{StaticResource HmiButtonStyle}">
        <Setter Property="Height" Value="36"/>
        <Setter Property="FontSize" Value="10"/>
    </Style>

    <!-- ===================== Run Button ===================== -->
    <Style x:Key="RunButtonStyle" TargetType="Button" BasedOn="{StaticResource HmiButtonStyle}">
        <Setter Property="Width"  Value="70"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Foreground" Value="{StaticResource StateRunningBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource StateRunningBrush}"/>
    </Style>

    <!-- ===================== Channel (소형 영상) 선택 버튼 ===================== -->
    <Style x:Key="ChannelButtonStyle" TargetType="Button">
        <Setter Property="Background"  Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderNormalBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor"      Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource NeonBlueBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== IP TextBox ===================== -->
    <Style x:Key="IpTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background"   Value="{StaticResource BgDarkBrush}"/>
        <Setter Property="Foreground"   Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="BorderBrush"  Value="{StaticResource BorderNormalBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontFamily"   Value="Consolas"/>
        <Setter Property="FontSize"     Value="12"/>
        <Setter Property="Padding"      Value="6,3"/>
        <Setter Property="CaretBrush"   Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource NeonBlueBrush}"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource NeonBlueBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===================== Status Label ===================== -->
    <Style x:Key="StatusLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"  Value="Consolas"/>
        <Setter Property="FontSize"    Value="11"/>
        <Setter Property="FontWeight"  Value="Bold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ===================== Section Label ===================== -->
    <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"  Value="Consolas"/>
        <Setter Property="FontSize"    Value="9"/>
        <Setter Property="Foreground"  Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <!-- ===================== Main Video Label ===================== -->
    <Style x:Key="MainVideoLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily"  Value="Consolas"/>
        <Setter Property="FontSize"    Value="13"/>
        <Setter Property="FontWeight"  Value="Bold"/>
        <Setter Property="Foreground"  Value="{StaticResource NeonCyanBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin"      Value="8,6,0,0"/>
    </Style>

    <!-- ===================== Panel Border ===================== -->
    <Style x:Key="PanelBorderStyle" TargetType="Border">
        <Setter Property="BorderBrush"     Value="{StaticResource BorderNormalBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background"      Value="{StaticResource BgPanelBrush}"/>
    </Style>

</ResourceDictionary>

<UserControl x:Class="NumPadDemo.Controls.NumPad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             d:DesignHeight="320" d:DesignWidth="260">

    <UserControl.Resources>
        <Style x:Key="NumPadButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FFE6E6E6"/>
            <Setter Property="Background" Value="#FF1A1A1A"/>
            <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Border x:Name="pressOverlay"
                                        Background="#FFFFFFFF"
                                        CornerRadius="2"
                                        Opacity="0"
                                        IsHitTestVisible="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="pressOverlay"
                                                          Storyboard.TargetProperty="Opacity"
                                                          From="0.35" To="0" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FunctionButtonStyle" TargetType="Button" BasedOn="{StaticResource NumPadButtonStyle}">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="#FF262626"/>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource FunctionButtonStyle}">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Normal"/>
        </Style>

        <Style x:Key="DisplayTextBoxStyle" TargetType="TextBox">
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="Foreground" Value="#FFE6E6E6"/>
            <Setter Property="Background" Value="#FF101010"/>
            <Setter Property="BorderBrush" Value="#FF3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="#FFE6E6E6"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="DisplayTextBox"
                 Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                 Text="0"
                 Style="{StaticResource DisplayTextBoxStyle}"/>

        <Button Grid.Row="1" Grid.Column="0" Content="7" Tag="7" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="1" Grid.Column="1" Content="8" Tag="8" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="1" Grid.Column="2" Content="9" Tag="9" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="1" Grid.Column="3" Content="&#xE94F;" Style="{StaticResource IconButtonStyle}" Click="CancelButton_Click" ToolTip="취소 (한 글자 지우기)"/>

        <Button Grid.Row="2" Grid.Column="0" Content="4" Tag="4" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="2" Grid.Column="1" Content="5" Tag="5" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="2" Grid.Column="2" Content="6" Tag="6" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="2" Grid.Column="3" Content="+/-" Style="{StaticResource FunctionButtonStyle}" Click="SignButton_Click"/>

        <Button Grid.Row="3" Grid.Column="0" Content="1" Tag="1" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="3" Grid.Column="1" Content="2" Tag="2" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="3" Grid.Column="2" Content="3" Tag="3" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="3" Grid.Column="3" Content="Clear" Style="{StaticResource FunctionButtonStyle}" Click="ClearButton_Click"/>

        <Button Grid.Row="4" Grid.Column="0" Content="." Style="{StaticResource NumPadButtonStyle}" Click="DecimalButton_Click"/>
        <Button Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Content="0" Tag="0" Style="{StaticResource NumPadButtonStyle}" Click="NumberButton_Click"/>
        <Button Grid.Row="4" Grid.Column="3" Content="Enter" Style="{StaticResource FunctionButtonStyle}" Click="EnterButton_Click"/>
    </Grid>
</UserControl>

<Window x:Class="NumPadDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:NumPadDemo.Controls"
        Title="NumPad Demo" Height="480" Width="360">
    <Window.Background>
        <SolidColorBrush Color="Black"/>
    </Window.Background>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="ValueTextBox1"
                 Grid.Row="0"
                 Height="36"
                 Margin="0,0,0,8"
                 FontSize="18"
                 VerticalContentAlignment="Center"
                 Text="0"/>

        <TextBox x:Name="ValueTextBox2"
                 Grid.Row="1"
                 Height="36"
                 Margin="0,0,0,16"
                 FontSize="18"
                 VerticalContentAlignment="Center"
                 Text="0"/>

        <!-- NumPad is a UserControl: drop it into any Grid cell and it stretches to fit. -->
        <controls:NumPad Grid.Row="2"/>
    </Grid>
</Window>
